{
  "name": "inter-code-nest-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "start cmd /k \"cd frontend && npm run dev\" && start cmd /k \"cd backend && npm run start:dev\"",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start:dev --prefix backend"
  }
}
